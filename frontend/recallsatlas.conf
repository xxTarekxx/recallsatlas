<VirtualHost *:80>
    ServerName recallsatlas.com
    ServerAlias www.recallsatlas.com

    RewriteEngine On

    # Force HTTPS + www (ALL traffic)
    RewriteCond %{HTTPS} !=on [OR]
    RewriteCond %{HTTP_HOST} !^www\.recallsatlas\.com$ [NC]
    RewriteRule ^ https://www.recallsatlas.com%{REQUEST_URI} [L,R=301]

    ErrorLog ${APACHE_LOG_DIR}/recallsatlas_error.log
    CustomLog ${APACHE_LOG_DIR}/recallsatlas_access.log combined
</VirtualHost>