# Copy to deploy-backend.env.ps1 (same folder as deploy-backend.ps1). That file is gitignored.
#
# DESTRUCTIVE: deploy removes the entire remote $VPS_ROOT on the VPS, then uploads from this PC.
# Wrong VPS_HOST or VPS_ROOT can delete the wrong directory. Verify before every run.

$VPS_HOST = "root@216.225.194.194"
$VPS_ROOT = "/var/www/html/recallsatlas/backend"
# Optional:
# $VPS_SSH_KEY = "C:\path\to\private_key"
# $VPS_PM2_APP = "recallsatlas"
